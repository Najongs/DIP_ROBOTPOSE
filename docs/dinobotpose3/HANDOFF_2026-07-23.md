# DINObotPose3 논문 마무리 핸드오프 (2026-07-23)

토큰 소진으로 중단. 이 문서 하나로 이어서 진행 가능. 논문 본문 = `PAPER_OVERLEAF.tex`(Overleaf 컴파일). env = `/home/najo/.conda/envs/dino/bin/python`.

## 0. 현재 상태 (git)

- 최신 커밋 `65fb27d` (evidence 표들 + freeze 그림), 그 전 `73355d4` (critic 수정). 논문 파일 uncommitted 없음(clean).
- **핵심 자산 = Panda real 4카메라 mean ADD-AUC 80.4 (SOTA vs RoboPEPP 78.0). 이건 아래 공정성 수정과 무관하게 견고.**
- **멀티로봇(KUKA/Baxter) 수치는 공정성 재측정 중** — 아래가 남은 작업의 전부.

## 1. 공정성 감사 결론 (사용자 지적으로 발견, `_debate_tmp/kuka_gate/audit_logs/`)

레퍼런스(RoboPEPP/HoRoPose/DREAM)는 **전체 테스트셋 + 모든 키포인트 + deterministic**로 평가. 우리와 비교:
- ✅ ADD-AUC 공식 동일. ✅ KUKA/Baxter 완전 결정론적(seed 무관, bit-identical). ✅ Panda held-out-30%는 full보다 ~1pt 더 어려움(보수적, 유리하지 않음).
- ❌ **문제 1 — metric mask**: 논문 KUKA/Baxter 수치(`u1_solver_vs_direct.py`)가 `valid_mask`(화면안 키포인트만) 평균 = RoboPEPP의 all-keypoints보다 관대. 교정 시 **KUKA 69.0→67.6, Baxter 71.3→68.7**(둘 다 결정론적).
- ❌ **문제 2 — Baxter 키포인트 셋(치명)**: 우리=**왼팔 7kp**(left_s0..w2), RoboPEPP=**전신 17kp**. 비교 불가 → **17-kp 전신 재학습으로 공정화 중**(§3).

## 2. 남은 논문 수정 (Baxter 17-kp 수치 나오면 한 번에 반영)

### (A) KUKA: Table 1에서 69.0 → **67.6** (all-keypoints, 결정론적)
```bash
cd 3_pose_models/DINObotPose3/Eval
python kuka_add_eval.py \
  --detector  ../TRAIN/outputs_heatmap/kuka_dream_detector_20260709_183119/best_heatmap.pth \
  --angle-head ../TRAIN/outputs_angle/kuka_angle_20260712_060212/best_angle_head.pth \
  --rot-head  ../TRAIN/outputs_rotation/kuka_rot_20260712_060214/best_rot_head.pth \
  --val-dir ../../../datasets/synthetic/kuka_synth_test_dr --max-frames 6000
# -> 0.6758 = 67.6 (all-keypoints). Baxter 붕괴 표(tab:baxtercollapse) KUKA 값도 동기화.
```

### (B) Baxter: Table 1 + tab:baxtercollapse → **17-kp 전신 재학습값** (§3에서 산출)
- 캡션에 반드시 명시: "**ADD-AUC over all keypoints (RoboPEPP convention)**; KUKA 7 keypoints (link1–7) vs RoboPEPP 8 (base omitted, conservative); Baxter 17 keypoints (whole body)."

### (C) 기여(iii) 완화 — wash-out 클레임 반증됨 (Batch C, `_debate_tmp/occ_washout/logs/FINAL_TABLE.txt`)
- 측정: pure self-train(증강 미유지) ≈ 배포(유지). 격차 A−B가 0/20/40%에서 +0.0075/+0.0046/+0.0087로 **일정**(가림에 안 벌어짐). "self-train이 가림 강건성을 씻어낸다"는 **근거 없음**. 강건성은 **처음부터 가림-증강된 head warm-start**에서 옴(sticky).
- 편집: abstract·intro 기여(iii)·Method §Occlusion(L118 부근)에서 "pure self-training would otherwise erode it" 삭제 → "retaining occlusion augmentation and synthetic data during self-training preserves clean-domain accuracy, while the occlusion robustness itself is largely conferred by the augmented head and retained regardless."

### (D) Panda synth: 1000-부분집합 → 전체 5997 재평가 (부분집합 caveat 제거; 값은 ~70.4/73.8±소폭 예상)
```bash
# selfbbox_eval.py 로 panda_synth_test_dr / _photo 전체(5997)를, --max-frames 6000, 부분집합 옵션 없이.
# 값 바뀌면 tab:main 및 tab:synthgap 의 70.4/73.8 갱신.
```

### (E) (선택) rigor critic 잔여 P0/P1 — `PAPER_CONSOLIDATION_2026-07-22.md` 및 감사 리포트 참조
- P0-3 backbone 표에 **frozen-pose-ADD 행** 추가(해리 명시): frozen DINOv3 pose(=배포 80.4) vs unfrozen(74.2) 대조.
- P1-4 **성분×가림 표**(cov-PnP/conf-gate/occ-aug를 0/20/40%에서 leave-one-out).
- P0-2 σ 반복 불필요(감사가 KUKA/Baxter 결정론적 확인; Panda만 ~1pt, 이미 disclosed).

## 3. Baxter 17-kp 전신 재학습 (진행 중 — 이어서 완료)

**목표**: RoboPEPP와 동일한 17 키포인트로 Baxter 재학습 → all-keypoints ADD로 공정 비교.
17 kp = `torso_t0` + 왼팔 8(s0,s1,e0,e1,w0,w1,w2,hand) + 오른팔 8(동일) (`RoboPEPP/ijepa/src/datasets/dream.py:154`).

- **FK 완성됨**: `TRAIN/model_v4.py:662` `baxter_forward_kinematics` (17 kp 출력). ⚠️ **먼저 FK가 GT 17 키포인트를 sub-mm로 재현하는지 검증**(과거 데이터-면책과 동일 방식) — 안 되면 그 키포인트 원인부터.
- **검출기 학습 중**: `TRAIN/outputs_heatmap/baxter_fullbody_detector_20260723_132444` (17채널). Ep1 2D AUC 0.29, L2 꼬리 ~125px = **양팔+말단 혼동으로 전신이 매우 어려움**(예상대로).
- **이어서 할 것**: 검출기 수렴 → 그 위에 angle head(양팔 14관절) + rot head 학습 → `baxter_add_eval.py`를 17-kp로 확장해 all-keypoints ADD 평가(full 5982, solver+true-K, deterministic).
- **정직한 전망**: 전신 17-kp는 RoboPEPP 34.4 수준으로 떨어질 가능성 큼. 그럼 **Baxter "1위" 주장 철회** → "동일 알고리즘이 전신 Baxter로 확장, SOTA와 대등/근접"으로 서술. (핵심 서사 = Panda real SOTA + 하나의 알고리즘 이식성.)

학습 명령 템플릿(왼팔 버전에서 --keypoint-names 17개로 확장):
```bash
# 검출기: TRAIN/train_heatmap.py --data-dir .../baxter_synth_train_dr --keypoint-names torso_t0,left_s0,...,right_hand
# 각도:   TRAIN/train_angle.py   (frozen 17-kp detector 위, angle-joints=양팔 14)
# 회전:   TRAIN/train_rotation.py
# GPU: UUID 지정(CLAUDE.md). free memory 기준.
```

## 4. 코드·아티팩트 위치

- **FK**: `TRAIN/model_v4.py` — `panda_forward_kinematics`(522), `iiwa7_forward_kinematics`(561), `baxter_left_forward_kinematics`(594), **`baxter_forward_kinematics`(662, 신규 17-kp)**.
- **Eval**: `Eval/{kuka_add_eval,baxter_add_eval,selfbbox_eval,refine_eval}.py`. `add_auc` 공식 = RoboPEPP와 동일(감사 확인).
- **감사 스크립트/로그**: `Eval/_debate_tmp/kuka_gate/` (`seed_run.py`, `audit_sweep.sh`, `mask_probe.py`, `rot_spread.sh`, `audit_logs/`).
- **Batch C(wash-out)**: `Eval/_debate_tmp/occ_washout/` (`sweep.sh`, `logs/FINAL_TABLE.txt`).
- **실패 시각화**: `Eval/failure_viz/panda_synth_{1_catastrophic,2_typical,3_fully_occluded}.png`.
- **freeze 그림 스크립트**: `docs/dinobotpose3/figures/make_fig_freeze_curve.py` → `fig_freeze_curve.pdf`.
- **진단 기록**: `experiments/2026-07-22_gap_reexamination.md`(§14–21.6: 데이터면책·2레버·freeze곡선·회수율게이트·Baxter기전·KUKA게이트), `experiments/SOTA_CAMPAIGN.md`, `PAPER_CONSOLIDATION_2026-07-22.md`.

## 5. 논문 서사 요약 (확정된 뼈대)

1. **방법/강점**: frozen DINOv3 + 운동학 솔버 + test-time RC = 해석적·스케일불변 깊이. **Panda real 80.4 SOTA**.
2. **분석(기여)**: synth 격차=100% 각도추론(oracle-angle 86.1>프론티어 83.0), joint-solve가 각도를 2D에서 재유도해 천장(freeze 곡선 concave), co-train은 real 정밀도와 트레이드오프. `tab:synthgap`+`fig:freeze_curve`로 뒷받침.
3. **멀티로봇**: 하나의 알고리즘, 로봇별 가중치+FK. **공정 규약(all-keypoints, 동일 키포인트 셋)으로 정직 비교** ← 지금 마무리 중.

## 6. 주의 (CLAUDE.md)

- push는 `origin=Najongs/DIP_ROBOTPOSE`만. 아카이브 repo push 금지. commit은 요청 시만.
- figure PDF 커밋은 `git add -f` (`.gitignore`가 *.pdf 무시).
- GPU는 UUID 지정. datasets/·1_capture/는 NAS 유일본 삭제금지. 루트 DREAM/RoboPEPP/robopose는 각자 .git 보유 → 루트 `git add -A` 금지.
